import 'package:flutter/material.dart';

class DatoxColors {
  static const primary = Color(0xFF2D3A8C); // Indigo
  static const accent = Color(0xFF2EC4B6); // Calm teal
  static const danger = Color(0xFFE63946);

  static const bg = Color(0xFFF8FAFC);
  static const surface = Colors.white;
  static const textPrimary = Color(0xFF0F172A);
  static const textMuted = Color(0xFF64748B);
}

class DatoxRadii {
  static const sm = Radius.circular(12);
  static const md = Radius.circular(16);
  static const lg = Radius.circular(24);
}

class DatoxSpacing {
  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 16.0;
  static const lg = 24.0;
  static const xl = 32.0;
}
