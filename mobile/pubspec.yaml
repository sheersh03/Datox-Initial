name: datox
description: Dating without the toxicity
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State + navigation
  flutter_riverpod: ^2.5.1
  go_router: ^14.6.2
  purchases_flutter: ^8.0.0

  # Network
  dio: ^5.7.0
  web_socket_channel: ^2.4.0

  # Storage
  flutter_secure_storage: ^9.2.2

  # UI
  flutter_animate: ^4.5.0
  cached_network_image: ^3.4.1
  font_awesome_flutter: ^10.7.0
  passkeys: ^2.17.4

  # Utils
  geolocator: ^13.0.2
  intl: ^0.19.0
  uuid: ^4.4.2

  # RevenueCat

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
