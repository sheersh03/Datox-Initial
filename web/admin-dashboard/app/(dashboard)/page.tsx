export default function OverviewPage() {
    return (
      <div>
        <h2 style={{ fontSize: 20, fontWeight: 800 }}>Overview</h2>
        <p style={{ marginTop: 8, opacity: 0.8 }}>
          Phase 1 scaffold. Next we’ll add real widgets: open reports count, bans, subscriptions snapshot, etc.
        </p>
      </div>
    );
  }